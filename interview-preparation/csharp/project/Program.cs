using System;
using project.Arrays;
using project.Collections;
using project.Delegates;
using project.Events;
using project.Extensions;
using project.iterators;
using project.Operator;
using project.Strings;

namespace project
{
    class Program
    {
        static void Main(string[] args)
        {
        //    new YieldReturn().Start();
        //    new DelegatesImplements().Process();
           // new ExtensionVsInstanceImplementation().TestMethods();
        //    new QueueExample().ProcessQueue();
        //    new StackExample().ProcessStack();
           // new ThreeDimensionalArray().Create();
            //new StringNumericRepresentation().Display();
            //new IndexerDemo().Test();
            //new EventHandlerTests().Test();\
            new OperatorOverloading().Test();
        }
    }
}
